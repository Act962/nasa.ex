services:
  postgres:
    image: "pgvector/pgvector:pg17"
    container_name: nasa-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: nasa_db
