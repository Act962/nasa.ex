# Shell scripts e Dockerfiles SEMPRE com LF — senão `env` no container quebra
# com erro `bash\r: No such file or directory` em hosts Windows.
*.sh        text eol=lf
Dockerfile  text eol=lf
entrypoint  text eol=lf
